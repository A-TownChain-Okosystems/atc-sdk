# Engineering Audit — 2026-09-16

## Scope

Documentation/tree consistency, architecture boundaries, build instructions, security documentation, and evidence status for `atc-sdk`.

## Findings

| ID | Area | Finding | Disposition |
|---|---|---|---|
| SDK-DOC-001 | Build | Root README instructed `cargo build --workspace` although no root `Cargo.toml` exists | Fixed |
| SDK-DOC-002 | Architecture | Architecture documented obsolete root `rust/` and `typescript/` paths | Fixed |
| SDK-DOC-003 | Structure | Active implementation is module-scoped under `modules/` | Documented |
| SDK-VER-001 | Verification | Current CI result was not available through the connected status endpoint | Open external evidence gate |
| SDK-SEC-001 | Security | `SECURITY.md` is the required vulnerability-reporting path | Verified by repository documentation |
| SDK-INT-001 | Integration | Cross-repo compatibility with current VM/node interfaces still requires a fresh CI/integration run | Open verification gate |

## Changes

- Rewrote root `README.md` to match the current module layout.
- Corrected `ARCHITECTURE.md` to remove obsolete root-level module claims.
- Updated `STATUS.md` with findings, remediation, and remaining evidence gates.

## Verification rule

A documentation correction is considered closed only when the referenced path exists on `main` and the corresponding build/test command is validated by CI or an equivalent reproducible test environment. This audit does not convert unavailable CI evidence into a PASS.
